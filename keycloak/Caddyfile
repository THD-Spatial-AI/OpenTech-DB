{$KEYCLOAK_DOMAIN} {
    encode zstd gzip

    @go_auth path /api/* /internal/*
    reverse_proxy @go_auth auth-service:8001

    reverse_proxy keycloak:8080

    header {
        X-Content-Type-Options nosniff
        Referrer-Policy strict-origin-when-cross-origin
        -Server
    }
}
