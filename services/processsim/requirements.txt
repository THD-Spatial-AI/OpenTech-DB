fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
psutil>=5.9
# Optional — only needed for PROCESS_ENGINE=openmodelica (the dynamic engine).
# Requires an OpenModelica install (omc) with a working C toolchain.
OMPython>=3.5

